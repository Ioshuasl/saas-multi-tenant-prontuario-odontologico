import { ProductionIndex } from '@/packages/financeiro/components/Production/ProductionIndex';

export const metadata = { title: 'Produção' };

export default function RelatoriosProductionPage() {
  return <ProductionIndex />;
}
