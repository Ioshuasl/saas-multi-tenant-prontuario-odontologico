import { AccountForm } from '@/packages/messaging/components/Account/AccountForm';

export const metadata = { title: 'WhatsApp' };

export default function WhatsappSettingsPage() {
  return <AccountForm />;
}
