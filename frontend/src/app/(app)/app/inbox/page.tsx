import { ConversationIndex } from '@/packages/messaging/components/Conversation/ConversationIndex';

export const metadata = { title: 'Inbox' };

export default function InboxPage() {
  return <ConversationIndex />;
}
